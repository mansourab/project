{{form_start(form)}}

    <div class="col-md-6 mt-4">
        {{ form_row(form.q) }}
        <button>Rechercher</button>
    </div>

    <div class="col-md-4">
        {{ form_row(form.categories) }}
    </div>

{{form_end(form)}}